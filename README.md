addpage
Added toolkit_add.php and db_connect.php.

Adjusted toolkit_add.html for adaptation. After multiple failed attempts to optimize the image upload function, the drag-and-drop image upload feature was removed, leaving only the click-to-upload option available.

Users can navigate to the add page from the seller page.

The add page is used for product uploads. 
Submission is only allowed after all fields marked with a red asterisk "*" are filled in. 
The submitted data will be stored in the local database, and images will be saved in a folder named upload.

Modify and adjust the code based on delivery2, add the corresponding database, and build a fully functional stationery sales website.
main
